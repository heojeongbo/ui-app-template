node_modules/
dist/
build/
coverage/

# Build info from `tsc -b` project references.
*.tsbuildinfo

# Intlayer's generated dictionary types. Produced by the vite plugin, never
# committed — see docs/i18n.md for why a cold checkout runs `pnpm intlayer`
# before `type:check`.
.intlayer/

# TanStack Router's plugin cache. `routeTree.gen.ts` itself IS committed.
.tanstack/

# Playwright
e2e/test-results/
e2e/playwright-report/
e2e/blob-report/
e2e/.auth/

# Local env overrides. `.env` and `.env.example` are committed.
.env.local
.env.*.local

# Dev TLS certs generated by scripts/ensure-dev-cert.sh
certs/

.DS_Store
*.log
