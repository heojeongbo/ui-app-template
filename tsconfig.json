{
	// Solution-style root: no files of its own, just the build graph. `tsc -b`
	// here type-checks every package in dependency order.
	"files": [],
	"references": [
		{ "path": "./packages/interfaces" },
		{ "path": "./packages/design" },
		{ "path": "./packages/core" },
		{ "path": "./apps/web" }
	]
}
